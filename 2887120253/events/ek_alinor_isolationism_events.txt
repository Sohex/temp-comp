namespace = alinor_isolationism
alinor_isolationism.0001 = { # Fullscreen Intro Event
	type = character_event
	window = fullscreen_event
	title = alinor_isolationism.0001.t
	desc = alinor_isolationism.0001.desc
	theme = realm
	trigger = { # we need to prevent this from firing for all players when a new player joins in MP
		is_ai = no
		NOT = { has_variable = alinor_isolationism_events_0001_var }
 	}
	override_background = { reference = ek_alinor }
	override_sound = { reference = "event:/DLC/FP2/SFX/UI/fp2_struggle_ui_intro_animate" }

	cooldown = { years = 100 }

	widgets = {
		widget = {
 			gui = "event_window_widget_situation_alinor_isolationism"
 			container = "dynamic_content_widget"
 			controller = situation_info
 			setup_scope = { situation:alinor_isolationism = { save_scope_as = situation } }
		}
	}
	immediate = {
		play_music_cue = mx_cue_peace_ensues
		set_variable = alinor_isolationism_events_0001_var
		save_scope_value_as = {
			name = start
			value = yes
		}
	}
	option = {
		name = alinor_isolationism.0001.a
		clicksound = "event:/DLC/FP2/SFX/UI/fp2_struggle_start_select"
	}
}

alinor_isolationism.0002 = {
	type = character_event
	title = alinor_isolationism.0002.t
	desc = {
		first_valid = {
			triggered_desc = {
				trigger = {
					any_character_situation = {
						any_situation_sub_region = {
							sub_region_current_phase = alinor_closed
						}
					}
				}
				desc = alinor_isolationism.0002.desc.closed
			}
			triggered_desc = {
				trigger = {
					any_character_situation = {
						any_situation_sub_region = {
							sub_region_current_phase = alinor_open
						}
					}
				}
				desc = alinor_isolationism.0002.desc.open
			}
		}
	}

	theme = realm

	left_portrait = {
		character = root
		animation = thinking
	}
	right_portrait = {
		character = scope:aran_el
		triggered_animation = {
			trigger = {
				any_character_situation = {
					any_situation_sub_region = {
						sub_region_current_phase = alinor_open
					}
				}
			}
			animation = personality_bold
		}
		animation = chancellor
	}

	trigger = {
		NOT = { has_variable = alinor_isolationism_game_start_var }
	}

	immediate = {
		custom_tooltip = alinor_isolationism.0002.tt
		play_music_cue = mx_cue_epic_sacral_moment
		random_ruler = {
			limit = {
				has_primary_title = title:e_summerset
			}
			save_scope_as = aran_el
		}
		scope:aran_el = {
			capital_county = { save_scope_as = aran_el_capital }
		}
	}

	option = {
		name = alinor_isolationism.0002.a
	}
}